import React from "react";

// Authentication removed - file deprecated. Keeping placeholder to avoid import errors if any lingering imports remain.
// You can delete this file entirely after ensuring no references.
export default function AuthProvider({ children }: any) {
  return <>{children}</>;
}
